# ListHelper
List helper. Currently with only the list label feature.  
Feature:
- Adds a list label for levels, showing how many created/favorite lists contains the level.  
- The label can be clicked to show what lists contains the level.  

<img src="logo.png" width="150" alt="the mod's logo" />

*Update logo.png to change your mod's icon (please)*

## Build instructions
Just see [geode docs](https://docs.geode-sdk.org/getting-started/create-mod#build)
```sh
# Assuming you have the Geode CLI set up already
geode build
```